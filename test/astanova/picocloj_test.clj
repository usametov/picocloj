(ns astanova.picocloj-test
  (:require [clojure.test :refer [deftest is testing]]
            [astanova.picocloj :as sut])) ; system under test

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
